# 📘 Инструкция по настройке авторизации через Госуслуги (ESIA)

## 🎯 Обзор

Данная инструкция поможет вам настроить полноценную OAuth 2.0 авторизацию через Госуслуги (Единая Система Идентификации и Аутентификации - ESIA) для вашего Telegram Mini App "Сетевой Город".

## 📋 Требования

- Зарегистрированное юридическое лицо или ИП
- Усиленная квалифицированная электронная подпись (УКЭП)
- Доступ к личному кабинету на портале Госуслуг

## 🚀 Пошаговая инструкция

### Шаг 1: Регистрация информационной системы

1. **Перейдите на портал технологической поддержки ЕСИА:**
   - Тестовая среда: https://esia-portal1.test.gosuslugi.ru
   - Продуктивная среда: https://esia.gosuslugi.ru

2. **Войдите в личный кабинет** с помощью учетной записи организации

3. **Зарегистрируйте новую информационную систему:**
   - Название: "Сетевой Город - Электронный дневник"
   - Тип: "Веб-приложение"
   - Описание: "Telegram Mini App для доступа к электронному дневнику"

4. **Укажите redirect URI:**
   ```
   Для разработки: http://localhost:3000/api/auth/esia/callback
   Для продакшена: https://yourdomain.com/api/auth/esia/callback
   ```

5. **Выберите scope (области доступа):**
   - ✅ openid - базовая информация
   - ✅ fullname - ФИО
   - ✅ birthdate - дата рождения
   - ✅ snils - СНИЛС
   - ✅ email - электронная почта
   - ✅ mobile - номер телефона

### Шаг 2: Получение учетных данных

После регистрации системы вы получите:

- **Client ID** - идентификатор клиента (например: `SNILS_ORG`)
- **Client Secret** - секретный ключ

💡 **Важно:** Сохраните эти данные в надежном месте!

### Шаг 3: Генерация сертификата (для продакшена)

Для работы в продуктивной среде требуется сертификат:

```bash
# Генерация приватного ключа
openssl genrsa -out esia_key.pem 2048

# Создание запроса на сертификат
openssl req -new -key esia_key.pem -out esia.csr

# Получение сертификата от УЦ
# Загрузите esia.csr в личный кабинет ЕСИА
# Скачайте подписанный сертификат как esia_cert.pem
```

### Шаг 4: Настройка переменных окружения

1. **Скопируйте файл примера:**
   ```bash
   cp .env.example .env
   ```

2. **Отредактируйте `.env` файл:**
   ```env
   # Базовая настройка
   PORT=3000
   
   # ESIA настройки
   ESIA_BASE_URL=https://esia-portal1.test.gosuslugi.ru  # или https://esia.gosuslugi.ru
   ESIA_CLIENT_ID=ваш_client_id
   ESIA_CLIENT_SECRET=ваш_client_secret
   ESIA_REDIRECT_URI=http://localhost:3000/api/auth/esia/callback
   
   # Сертификаты (для продакшена)
   ESIA_CERT_PATH=./esia_cert.pem
   ESIA_KEY_PATH=./esia_key.pem
   ```

3. **Установите зависимости:**
   ```bash
   npm install dotenv
   ```

4. **Обновите server.js:**
   ```javascript
   // В начале файла
   require('dotenv').config();
   ```

### Шаг 5: Тестирование

1. **Запустите сервер:**
   ```bash
   npm start
   ```

2. **Откройте браузер** и перейдите на `http://localhost:3000`

3. **Нажмите кнопку "Войти через Госуслуги"**

4. **Проверьте перенаправление** на страницу авторизации ЕСИА

5. **Авторизуйтесь** и проверьте callback

## 🔐 Безопасность

### ⚠️ Важные правила безопасности:

1. **Никогда не храните `CLIENT_SECRET` в коде!**
   - Используйте только переменные окружения
   - Добавьте `.env` в `.gitignore`

2. **Используйте HTTPS в продакшене:**
   ```nginx
   server {
       listen 443 ssl;
       ssl_certificate /path/to/cert.pem;
       ssl_certificate_key /path/to/key.pem;
       
       location / {
           proxy_pass http://localhost:3000;
       }
   }
   ```

3. **Проверяйте state параметр:**
   - Предотвращает CSRF атаки
   - Реализовано в коде автоматически

4. **Используйте PKCE:**
   - Code Challenge/Verifier уже реализован
   - Защищает от перехвата authorization code

## 📝 Пример использования API

### Инициализация авторизации:

```javascript
// GET /api/auth/esia/login?telegram_user_id=123456
{
  "success": true,
  "authUrl": "https://esia.gosuslugi.ru/aas/oauth2/ac?..."
}
```

### После успешной авторизации:

```javascript
// GET /api/auth/esia/user
// Authorization: Bearer session_id
{
  "success": true,
  "user": {
    "oid": "1000012345",
    "firstName": "Иван",
    "lastName": "Иванов",
    "middleName": "Иванович",
    "birthDate": "01.01.2000",
    "snils": "123-456-789 00",
    "email": "ivan@example.com",
    "mobile": "+79001234567"
  },
  "authType": "esia"
}
```

## 🔄 Обновление токена

Токены ESIA имеют ограниченный срок действия. Для обновления:

```javascript
// POST /api/auth/esia/refresh
// Authorization: Bearer session_id
{
  "success": true
}
```

## 🐛 Отладка

### Включение логов:

```javascript
// В server.js добавьте:
app.use((req, res, next) => {
    console.log(`${new Date().toISOString()} ${req.method} ${req.url}`);
    next();
});
```

### Проверка статуса:

```bash
curl http://localhost:3000/health
```

Ответ:
```json
{
  "status": "ok",
  "sessions": 0,
  "esiaEnabled": true
}
```

## 📚 Полезные ссылки

- [Портал ЕСИА](https://esia.gosuslugi.ru)
- [Технологический портал ЕСИА](https://esia-portal1.test.gosuslugi.ru)
- [Документация OAuth 2.0](https://oauth.net/2/)
- [Методические рекомендации ЕСИА](https://digital.gov.ru/ru/documents/6186/)

## ❓ Часто задаваемые вопросы

### В: Нужна ли УКЭП для тестирования?

О: Нет, для тестовой среды можно использовать тестовые учетные данные. УКЭП нужна только для продакшена.

### В: Как долго действует токен?

О: Access token обычно действует 1-2 часа. Используйте refresh token для обновления.

### В: Можно ли использовать для физических лиц?

О: Да, физические лица с подтвержденной учетной записью могут авторизоваться через Госуслуги.

### В: Что делать, если redirect не работает?

О: Проверьте, что redirect_uri в коде точно совпадает с тем, что указан в настройках ЕСИА (включая протокол, порт и путь).

## 🎉 Готово!

Теперь ваше приложение поддерживает полноценную авторизацию через Госуслуги. Пользователи могут войти как через логин/пароль СГО, так и через учетную запись Госуслуг.

## 💡 Дальнейшие улучшения

1. **Связывание аккаунтов:**
   - Позвольте пользователям связать аккаунт ЕСИА с СГО
   - Храните соответствие в базе данных

2. **Автоматический вход в СГО:**
   - После авторизации через ЕСИА автоматически логиньтесь в СГО
   - Используйте СНИЛС или email для поиска аккаунта

3. **Обработка ошибок:**
   - Добавьте детальную обработку различных типов ошибок
   - Логируйте все ошибки для анализа

4. **Мониторинг:**
   - Настройте мониторинг статуса ЕСИА
   - Отслеживайте успешность авторизаций

---

**Автор:** Telegram Mini App для Сетевого Города  
**Версия:** 2.0 с поддержкой Госуслуг  
**Дата:** 2026
