# 🎓 Telegram Mini App для Сетевой Город. Образование

Полнофункциональное Telegram Mini App с полной интеграцией API системы "Сетевой Город. Образование" (СГО).

## 🌟 Основные возможности

### ✅ Реализованные функции:
- ✨ **Авторизация** - вход через реальные учетные данные СГО
- 📖 **Дневник** - просмотр оценок и статистики
- 📅 **Расписание** - уроки на день/неделю
- 📊 **Оценки** - все оценки по предметам с расчетом среднего балла
- 📝 **Домашнее задание** - актуальные задания с дедлайнами
- 💾 **Сохранение сессии** - автоматический вход при повторном открытии
- 🎨 **Адаптация под Telegram** - поддержка светлой/темной темы
- 📱 **Мобильная оптимизация** - идеально работает на любых устройствах

## 🏗️ Архитектура

```
┌─────────────────┐
│  Telegram Bot   │
│   (Mini App)    │
└────────┬────────┘
         │
         │ HTTPS
         ▼
┌─────────────────┐
│  Backend API    │
│   (Node.js)     │
└────────┬────────┘
         │
         │ HTTPS
         ▼
┌─────────────────┐
│  СГО API        │
│  sgo.rso23.ru   │
└─────────────────┘
```

## 📋 Требования

- **Node.js** версии 16.x или выше
- **npm** или **yarn**
- **Telegram аккаунт**
- Доступ к интернету

## 🚀 Установка и запуск

### 1. Клонирование проекта

```bash
# Создайте папку для проекта
mkdir sgo-telegram-app
cd sgo-telegram-app

# Скопируйте файлы:
# - server.js
# - index.html
# - package.json
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Запуск backend сервера

```bash
# Режим разработки (с автоперезагрузкой)
npm run dev

# Или обычный запуск
npm start
```

Сервер запустится на порту **3000** (или указанном в переменной окружения PORT).

### 4. Настройка Telegram Mini App

#### 4.1. Создание бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните полученный **токен**

#### 4.2. Создание Mini App

1. Отправьте команду `/newapp` в @BotFather
2. Выберите созданного бота
3. Введите название приложения: **Сетевой Город**
4. Введите описание: **Электронный дневник**
5. Загрузите фото (опционально)
6. Введите короткое название: **sgo**

#### 4.3. Настройка URL

Вам нужен публичный URL для вашего сервера. Варианты:

**Вариант A: Локальная разработка (ngrok)**
```bash
# Установите ngrok
npm install -g ngrok

# Запустите туннель
ngrok http 3000

# Скопируйте HTTPS URL (например: https://abc123.ngrok.io)
```

**Вариант B: Облачный хостинг**

Разверните на:
- [Vercel](https://vercel.com)
- [Railway](https://railway.app)
- [Render](https://render.com)
- [Heroku](https://heroku.com)

#### 4.4. Обновление URL в коде

В файле `index.html` замените:
```javascript
const API_URL = 'http://localhost:3000'; // Замените на ваш публичный URL
```

На:
```javascript
const API_URL = 'https://your-server-url.com';
```

#### 4.5. Загрузка HTML

1. Загрузите `index.html` на хостинг (GitHub Pages, Vercel, Netlify и т.д.)
2. Получите публичный URL (например: `https://username.github.io/sgo-app/`)
3. В @BotFather отправьте `/setmenubutton`
4. Выберите вашего бота
5. Введите URL вашего Mini App

### 5. Запуск приложения

1. Откройте вашего бота в Telegram
2. Нажмите кнопку **Menu** или **Web App**
3. Приложение откроется!

## 🔧 Конфигурация

### Переменные окружения

Создайте файл `.env`:

```env
PORT=3000
NODE_ENV=production

# Опционально: Redis для хранения сессий
REDIS_URL=redis://localhost:6379

# Опционально: секретный ключ для JWT
JWT_SECRET=your-secret-key
```

### Настройка региона

В файле `server.js` можно настроить базовый URL:

```javascript
// Краснодарский край (по умолчанию)
const SGO_BASE_URL = 'https://sgo.rso23.ru';

// Или другие регионы:
// Москва: https://sgo.mos.ru
// Санкт-Петербург: https://sgo.spb.ru
```

## 📡 API Endpoints

### Авторизация
```
POST /api/login
Body: { username, password, region }
Response: { success, sessionId, user }
```

### Получение пользователя
```
GET /api/user
Headers: { Authorization: Bearer <sessionId> }
Response: { user data }
```

### Дневник
```
GET /api/diary?weekStart=YYYY-MM-DD&weekEnd=YYYY-MM-DD
Headers: { Authorization: Bearer <sessionId> }
Response: { diary data }
```

### Оценки
```
GET /api/grades?periodId=0
Headers: { Authorization: Bearer <sessionId> }
Response: { grades data }
```

### Расписание
```
GET /api/schedule?date=YYYY-MM-DD
Headers: { Authorization: Bearer <sessionId> }
Response: { schedule data }
```

### Домашнее задание
```
GET /api/homework?fromDate=YYYY-MM-DD&toDate=YYYY-MM-DD
Headers: { Authorization: Bearer <sessionId> }
Response: { homework data }
```

### Выход
```
POST /api/logout
Headers: { Authorization: Bearer <sessionId> }
Response: { success }
```

## 🛡️ Безопасность

### Текущая реализация (для разработки):
- ✅ Сессии хранятся в памяти сервера
- ✅ CORS настроен для всех источников
- ✅ Базовая валидация входных данных

### Рекомендации для production:
- 🔐 Используйте HTTPS для всех соединений
- 💾 Храните сессии в Redis
- 🔑 Добавьте JWT токены
- 🛡️ Настройте CORS только для вашего домена
- 📝 Добавьте rate limiting
- 🔒 Шифруйте чувствительные данные
- 📊 Добавьте логирование и мониторинг

## 🐛 Возможные проблемы и решения

### Ошибка CORS
**Проблема:** Браузер блокирует запросы
**Решение:** Убедитесь, что backend и frontend на HTTPS

### Ошибка авторизации
**Проблема:** Неверный логин/пароль
**Решение:** Проверьте учетные данные в СГО

### Не загружаются данные
**Проблема:** API СГО недоступен
**Решение:** Проверьте доступность сайта sgo.rso23.ru

### Сессия не сохраняется
**Проблема:** LocalStorage заблокирован
**Решение:** Проверьте настройки приватности браузера

## 📱 Тестирование

### Тестовый режим
Пока нет реальных данных, можно использовать демо-версию (закомментирована в коде).

### Проверка работы:
1. Откройте консоль разработчика (F12)
2. Проверьте Network tab на наличие запросов к API
3. Проверьте Console на ошибки

## 🎨 Кастомизация

### Изменение цветовой схемы

В `index.html` найдите:
```css
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

Замените на свои цвета.

### Добавление новых функций

1. Добавьте endpoint в `server.js`
2. Добавьте функцию в `index.html`
3. Обновите UI

## 📚 Документация API СГО

API "Сетевой Город. Образование" не имеет официальной документации, но основные endpoints:

- `/webapi/auth/login` - авторизация
- `/webapi/student/diary` - дневник
- `/webapi/student/grades` - оценки
- `/webapi/student/diary/assigns` - домашнее задание
- `/webapi/context` - контекст пользователя

## 🤝 Вклад

Приветствуются pull requests! Для больших изменений сначала откройте issue.

## 📄 Лицензия

MIT

## 🆘 Поддержка

Если у вас возникли проблемы:

1. Проверьте логи сервера
2. Проверьте консоль браузера
3. Убедитесь, что все зависимости установлены
4. Проверьте доступность СГО API

## 🎯 Roadmap

- [ ] Добавить уведомления о новых оценках
- [ ] Добавить графики успеваемости
- [ ] Добавить экспорт в PDF
- [ ] Добавить поддержку нескольких детей
- [ ] Добавить чат с учителями
- [ ] Добавить оплату питания
- [ ] Мультиязычность

## ⚡ Производительность

- Кэширование данных
- Lazy loading
- Оптимизация запросов
- Минификация JS/CSS

---

**Сделано с ❤️ для учеников, родителей и учителей**
